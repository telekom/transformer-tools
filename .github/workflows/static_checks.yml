name: Static Code Checks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # * is a special character in YAML so you have to quote this string
    # at 4 am
    - cron:  '0 4 * * *'

jobs:
  checks:
    env:
      src: "transformer_tools"
      other-src: "tests docs setup.py"

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
        python-version: 3.7

    # --use-deprecated=legacy-resolver is a temporary fix and should be disabled asap
    # it is to install spacy - see https://stackoverflow.com/a/65793484/271118
    - name: Install
      run: |
        python -m pip install -U pip
        pip install --progress-bar off -U .[checking,optional] --use-deprecated=legacy-resolver

    - name: Check with pydocstyle
      run: pydocstyle --count ${{ env.src }} ${{ env.other-src }}

    - name: Check with black
      run: black ${{ env.src }} ${{ env.other-src }} --check --diff

    - name: Check with flake8
      run: flake8 ${{ env.src }} ${{ env.other-src }}

    - name: Check with isort
      run: isort ${{ env.src }} ${{ env.other-src }} --check --diff

    - name: Check with mdformat
      run: mdformat --check *.md

    - name: Check with mypy
      run: mypy --install-types --non-interactive ${{ env.src }} ${{ env.other-src }}

    - name: Check with pylint
      run: pylint ${{ env.src }}
